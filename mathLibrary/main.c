#include <stdio.h>
#include <stdlib.h>
#include <math.h> //we have to add this library
int main()
{
   //ceil(prints ceil of the number)
   //floor(prints floor of the number)
   //pow(finds the power of the number)
   //sqrt(finds the square root of the number)
   //abs(finds the absolute value of the number)

   printf("%.2f\n",ceil(3.4));

   printf("%.2f\n",floor(3.4));

   printf("%.2f\n",pow(2,3));

   printf("%.2f\n",sqrt(81));

   printf("%d",abs(-156));
    return 0;
}
